#!/bin/bash
# Bash shim for cdda.py. Resolves cdda.py from this script's own directory
# so the repo can be moved or renamed without editing this file.
DIR="$(cd "$(dirname "$(readlink -f "$0" || echo "$0")")" && pwd)"
exec /c/Python313/python.exe "$DIR/cdda.py" "$@"
